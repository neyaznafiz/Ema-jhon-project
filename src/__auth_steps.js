/**
 * --------------- 10-4-2022-----------------
 * 1. create a new project firebase.google.com.
 * 2. npm install firebase.
 * 3. create firebase.init and import getAuth to export.
 * 4. go to authentication > enable Email and pass auth.
 * 5. create login signup component, setup route.
 * 6. attach form field handler and submit handler.
 * 7. install firebase hooks ( npm install --save .react-firebase-hooks )
 * 8. user create with email and pass ( useCreateUserWithEmailAndPassword ) from react-firebase-hooks.
 * 9. if user is created redirect to the expected page.
 * 10. (useSignInWithEmailAndPassword) for signin.
 * 11. create RequireAuth component ==> check user exist, tract user location.
 * 12. In rout wrap protected component by  using auth component
 */




/**
 * hosting steps
 * 1. npm install -g firebase-tools (first time)
 * 2. firebase login (first time )
 * 3. firebase init (one time for project)
 * 4. npm run build (every time to deploy)
 * 5. firebase deploy (every time to deploy)
*/